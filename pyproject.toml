[project]
name = "kami-chan-optimized"
version = "0.1.0"
description = "Tooling for updating outdated packwiz mods"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "GitPython==3.1.46",
    "tomli_w==1.2.0",
]

[tool.uv]
package = false
